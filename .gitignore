node_modules/
dist/
server/data/*.db-wal
server/data/*.db-shm
server/data/*.bak
!server/data/freeapi.db
.env
.env.local
.DS_Store

# Personal deployment scripts (contain keys/credentials — kept local)
deploy-pi.sh
update-hermes.sh

# Monetization planning — private, never committed
monetization/
